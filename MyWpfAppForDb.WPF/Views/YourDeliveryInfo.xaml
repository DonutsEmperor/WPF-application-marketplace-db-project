<UserControl x:Class="MyWpfAppForDb.WPF.Views.YourDeliveryInfo"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:controls="clr-namespace:MyWpfAppForDb.WPF.Controls"
		mc:Ignorable="d"
		MinHeight="450" MinWidth="800">

	<UserControl.Resources>
		<Style TargetType="TextBox">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="Height" Value="30"/>
			<Setter Property="MaxWidth" Value="300"/>
			<Setter Property="Margin" Value="5,0,0,0"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="HorizontalContentAlignment" Value="Left"/>
		</Style>

		<Style TargetType="TextBlock">
			<Setter Property="FontSize" Value="16" />
			<Setter Property="Height" Value="30"/>
			<Setter Property="MaxWidth" Value="300"/>
			<Setter Property="Margin" Value="10,0,0,0"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>

		<Style TargetType="Label">
			<Setter Property="Height" Value="35" />
			<Setter Property="Width" Value="200" />
			<Setter Property="FontSize" Value="20"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
		</Style>

		<Style TargetType="Button">
			<Setter Property="HorizontalAlignment" Value="Left" />
			<Setter Property="VerticalAlignment" Value="Bottom" />
			<Setter Property="Width" Value="60"/>
			<Setter Property="Height" Value="30"/>
			<Setter Property="Margin" Value="10"/>
		</Style>
	</UserControl.Resources>

	<Grid ShowGridLines="False">
		
		<Grid.RowDefinitions>
			<RowDefinition MinHeight="50"></RowDefinition>
			<RowDefinition MinHeight="40"></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition MinHeight="50"></RowDefinition>
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition></ColumnDefinition>
			<ColumnDefinition></ColumnDefinition>
		</Grid.ColumnDefinitions>

		<controls:SearchControl Grid.ColumnSpan="2" Grid.Row="0" HorizontalAlignment="Center" Margin="10"
			DataContext="{Binding SearchViewModel}"/>

		<Label Content="Заказы" Grid.Column="0" Grid.Row="1"/>
		<Label Content="Состав заказа" Grid.Column="1" Grid.Row="1"/>

		<DataGrid ItemsSource="{Binding Orders}" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Stretch" MinHeight="350"/>
		<DataGrid ItemsSource="{Binding Products}" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Stretch" MinHeight="350"/>

		<Button Content="Выбрать" Command="{Binding SelectOrder}" Grid.Column="0" Grid.Row="3"/>
		<Button Content="Применить" Command="{Binding SelectProduct}" Grid.Column="1" Grid.Row="3"/>
		
	</Grid>
	
</UserControl>
